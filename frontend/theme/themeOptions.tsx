import { createTheme  } from '@mui/material/styles';

export const DefaultTheme = createTheme({
    palette: {
        mode: 'dark',
        primary: {
            main: '#d32f2f',
            dark:"#272727"
        },
    }
});